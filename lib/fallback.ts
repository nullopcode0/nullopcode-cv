import type { Profile, Project, Contribution } from './types';

export const fallbackProfile: Profile = {} as Profile;
export const fallbackProjects: Project[] = [];
export const fallbackContributions: Contribution[] = [];
